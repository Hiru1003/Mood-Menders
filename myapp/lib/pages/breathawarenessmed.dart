import 'package:flutter/material.dart';

class breathmed_page extends StatefulWidget {
  const breathmed_page({
    super.key,
  });

  @override
  State<breathmed_page> createState() => _breathmed_pageState();
}

class _breathmed_pageState extends State<breathmed_page> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
